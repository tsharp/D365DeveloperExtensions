<UserControl x:Class="WebResourceDeployer.WebResourceDeployerWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WebResourceDeployer"
             xmlns:vsfx="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:core="clr-namespace:CrmDeveloperExtensions.Core;assembly=CrmDeveloperExtensions.Core"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="1100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <core:XrmToolingConnection Grid.Column="0" Name="ConnPane" Connected="ConnPane_OnConnected"></core:XrmToolingConnection>
        <Grid Column="1" HorizontalAlignment="Stretch">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
            </StackPanel>
            <DataGrid Background="{DynamicResource {x:Static vsfx:VsBrushes.ToolWindowBackgroundKey}}"></DataGrid>
            <TextBox HorizontalAlignment="Left" Height="23" Margin="477,172,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="120"/>
        </Grid>
    </Grid>
</UserControl>
